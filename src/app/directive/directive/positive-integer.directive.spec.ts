import { PositiveIntegerDirective } from './positive-integer.directive';

describe('PositiveIntegerDirective', () => {
  it('should create an instance', () => {
    const directive = new PositiveIntegerDirective();
    expect(directive).toBeTruthy();
  });
});
